const promisePoints = require('../components/promisePoints');

module.exports = {
  id: 'promise',
  title: 'Promise & Approach',
  details: promisePoints,
  describe() {
    return `${this.title}: steady support and clear expectations.`;
  },
};
